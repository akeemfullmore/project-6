const qwerty = document.getElementById('qwerty');
const phrase = document.getElementById('phrase');
const missed = 0;
const startBtn = document.getElementsByClassName('btn_reset');

startBtn.addEventListener('click', (e) => {
    console.log(startBtn);

});